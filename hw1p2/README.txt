把模型檔案存到 workingDir 裡面
然後開啟 FileName.cpp ，更改裡面的檔名

注意：
這個程式使用了 Corona 讀圖函式庫，也許需要重新編譯 Corona
Corona 在 <http://corona.sf.net>，還有 <https://github.com/chadaustin/corona>

程式可以支援 Linux 了！要在 Ubuntu 下開啟，需要有 libpng、libjpeg、freeglut3-dev、libglew-dev，只要在命令列輸入 make 就可以編譯。程式檔會放在 workingDir 裡
